{
  "name": "loop-surfers",
  "version": "0.1.0",
  "description": "Safe continuation-loop skills and hooks for Codex and Claude Code.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/sabyunrepo/loop-surfers.git"
  },
  "homepage": "https://github.com/sabyunrepo/loop-surfers",
  "bin": {
    "agent-loop": "./bin/agent-loop.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/agent-loop.js && node --check src/cli.js && node --check src/kernel/state.js && node --check src/kernel/blockers.js && node --check src/kernel/scheduler.js && node --check src/kernel/prompts.js"
  },
  "files": [
    "bin/",
    "src/",
    "docs/",
    "examples/",
    "README.md",
    "LICENSE",
    "setup"
  ],
  "keywords": [
    "codex",
    "claude-code",
    "hooks",
    "agent",
    "automation",
    "loop"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
